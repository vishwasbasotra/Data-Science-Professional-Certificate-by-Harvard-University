# 'length.out' is used to make sequence of specified length


x <- seq(0, 100, length.out = 5)
x
length(x)

# Store the sequence in the object a
a<-seq(1,10,length.out=100)
a
length(a)

# Determine the class of a
class(a)