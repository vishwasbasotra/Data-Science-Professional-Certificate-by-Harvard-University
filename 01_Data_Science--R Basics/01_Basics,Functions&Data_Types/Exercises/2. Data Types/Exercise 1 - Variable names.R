"For this question, we will use the names() function to 
extract the column names of the murders data frame."

# Load package and data

library(dslabs)
data(murders)

# Use the function names to extract the variable names 
names(murders)
